# Key List Expression (keylix)

`keylix` is a simple pattern notation used for searching keys (strings) in a list.
It allows writing intuitive patterns that match subsets of elements in a list of strings.
